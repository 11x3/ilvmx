"about" |> Signal.m(Program.cmd fn s -> Item.m Markdown.to_html """
#Cakedown

Cakedown is one of many [magic programming languages](http://en.wikipedia.org/wiki/Magic_(programming)) you use to create cloud apps in seconds, writing them with only simple words and easy commands to program them.

> In the context of computer programming, magic [programming] is an informal term for abstraction - it is used to describe code that handles complex tasks while hiding that complexity to present a simple interface.

Program from anywhere you can type a small tweet or txt or even on a Mac Pro with thousands of tagged and encrypted items.
"""