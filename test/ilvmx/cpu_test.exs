defmodule CPUTest do
  use   ExUnit.Case, async: true

  # test "Castle.CPU.execute! to exe a `Signal` on the CPU.",
  #   do: assert %Signal{path: "lol", let: "nub"} = Castle.CPU.execute! Signal.m("lol", "nub")
  #
end